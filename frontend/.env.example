# Vite build-time API base (no trailing slash).
# Local: leave unset (defaults to http://localhost:8000) or set explicitly.
# Vercel Production: set in the Vercel dashboard, e.g.
#   VITE_API_URL=https://YOUR-BACKEND.up.railway.app
VITE_API_URL=http://localhost:8000
